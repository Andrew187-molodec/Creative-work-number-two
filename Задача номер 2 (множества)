 #Дан список фамилий, который могут содержать до 10 фамилий. Определить, сколько в нём встречается однофамильцев.
#Ввод:
#Некторый обьём фамилий.
#Вывод:
#Все повторяющиеся фамилии через запятую. 
Решение:
def find_duplicate_surnames():
    surnames = input("Введите фамилии через запятую (до 10): ").strip().split(',')
    surnames = {surname.strip() for surname in surnames}  # Используем множество для уникальности

    if len(surnames) > 10:
        print("Пожалуйста, введите не более 10 фамилий.")
        return
    seen = set()
    duplicates = set()
    for surname in surnames:
        if surname in seen:
            duplicates.add(surname)
        else:
            seen.add(surname)
    if duplicates:
        print("Повторяющиеся фамилии:", ', '.join(duplicates))
    else:
        print("Нет повторяющихся фамилий.")
#Тестовые данные:
1)Иванов, Петров, Сидоров, Иванов, Смирнов, Петров
1) Ответ: Повторяющиеся фамилии: Иванов, Петров
2)Иванов, Петров, Сидоров, Иванов, Смирнов, Петров, Галкин, Галкин
2)Ответ: Повторяющиеся фамилии: Иванов, Петров, Галкин
